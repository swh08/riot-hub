services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: riot_hub
      POSTGRES_USER: riot
      POSTGRES_PASSWORD: riot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
